<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="item"
            type="com.example.todo_mvvm.db.TodoModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/todo_recyclerview_background"
        android:padding="12dp"
        android:layout_marginBottom="18dp">

        <TextView
            android:id="@+id/categoryTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="Category"
            android:text="@{item.folder}"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <CheckBox
            android:id="@+id/todoCheck"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginTop="8dp"
            android:background="@drawable/todo_check_selector"
            android:button="@null"
            android:checked="@{item.check}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/categoryTextView" />

        <TextView
            android:id="@+id/todoTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{item.todo}"
            todoCheck="@{item.check}"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="@id/todoCheck"
            app:layout_constraintEnd_toStartOf="@id/startDayTextView"
            app:layout_constraintStart_toEndOf="@id/todoCheck"
            app:layout_constraintTop_toTopOf="@id/todoCheck"
            tools:text="06/29~06/30 Todo" />

        <TextView
            android:id="@+id/endDayTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            dateString="@{item.end_day}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/todoCheck"
            tools:text="06-29" />

        <TextView
            android:id="@+id/tv_dash"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="4dp"
            android:text="~"
            app:layout_constraintBottom_toBottomOf="@id/startDayTextView"
            app:layout_constraintEnd_toStartOf="@id/endDayTextView"
            app:layout_constraintTop_toTopOf="@id/startDayTextView" />

        <TextView
            android:id="@+id/startDayTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="4dp"
            dateString="@{item.start_day}"
            app:layout_constraintEnd_toStartOf="@id/tv_dash"
            app:layout_constraintTop_toTopOf="@id/todoCheck"
            tools:text="06-28" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="@id/todoTextView"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/todoTextView"
            android:text="@{item.description}"
            android:layout_marginTop="4dp"
            tools:text="Todo의 설명란입니다." />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>